// Identifiers for every sound effect in the game.
// Add new entries at the end so existing serialized references in SFXConfig stay valid.
public enum SFXType
{
    Tap,
    PlacePuppy,
    Error,
    Win,
    Lose,
    ButtonClick,
    Confetti
}
