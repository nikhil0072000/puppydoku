// Identifiers for every visual effect spawned at runtime.
// Add new entries at the end so existing serialized references in VFXConfig stay valid.
public enum VFXType
{
    Confetti,
    Pop,
    Dust,
    RedCross,
    Sparkle
}
